{
    "schemaVersion": 1,
    "upstream": {
        "repository": "https://github.com/github/cmark-gfm",
        "version": "0.29.0.gfm.13",
        "commit": "587a12bb54d95ac37241377e6ddc93ea0e45439b",
        "released": "2023-07-21",
        "note": "The newest tag upstream has published. Upstream has been dormant since, so this pin is not expected to move often; bumping it is a reviewed change because it redefines what parity means."
    },
    "claim": "Markdown Core's Markdown semantics are identical to upstream cmark-gfm's, except for the deltas below. The check that enforces it is scripts/check-upstream-parity.mjs.",
    "deltas": [
        {
            "id": "own-extensions",
            "kind": "excluded-from-comparison",
            "summary": "Markdown Core adds formula, directive, cross_link, and embed syntax that upstream does not parse.",
            "handling": "The comparison runs this repository's parser under its `gfm` profile, which leaves all four off, so both sides parse the same language.",
            "evidence": "packages/markdown-core/core/main.c, --profile gfm"
        },
        {
            "id": "footnote-resolution-model",
            "kind": "deliberate-difference",
            "summary": "Upstream unlinks a footnote definition nothing refers to. Markdown Core keeps every definition where it was written, referenced or not; whether [^label] is a footnote at all is answered by the whole-document definition set, and no numbering is projected.",
            "rationale": "An unreferenced definition is text the author wrote; dropping it makes the tree stop describing the source, and makes whether a block exists depend on an inline elsewhere in the document.",
            "support": "remark keeps unreferenced definitions too, so this half has a second authority \u2014 recorded as `footnote-definition-placement` in specs/mdast-parity/deltas.json.",
            "handling": "The comparison drops unreferenced definitions from this side before comparing, so what is checked is the resolved language rather than the two representations of retention.",
            "evidence": "packages/markdown-core/core/parser.h, footnote_defs; packages/markdown-core/core/references.c, markdown_core_footnote_definition_create; scripts/lib/upstream-cmark.mjs, applyUpstreamFootnoteModel",
            "history": "Until 2026-08-02 this also covered unresolved references, which upstream rewrites to literal text and this repository kept as nodes. That half was given up rather than defended: it made footnote references the one reference form a renderer had to special-case, and both authorities already agreed against it. The two sides now produce the same text for an undefined `[^label]`, label unparsed, with nothing projected."
        },
        {
            "id": "footnote-definition-placement",
            "kind": "deliberate-difference",
            "summary": "Upstream moves every footnote definition to the document tail in first-reference order. Markdown Core's AST is source-faithful and leaves each definition where it was written.",
            "handling": "Both sides have their footnote definitions lifted out and re-attached in one deterministic order, so their content is compared and their position is not.",
            "evidence": "docs/specs/canonical-ast.md, 'A footnote definition is an ordinary block at its source position: it is never moved to the document tail'."
        },
        {
            "id": "reference-definition-node",
            "kind": "deliberate-difference",
            "summary": "Upstream consumes a link reference definition into its reference map, leaving no node, and resolves every reference into a Link or Image carrying a copy of the destination. Markdown Core keeps the definition as a ReferenceDefinition block where it was written, and a reference carries only its label and the form it was written in.",
            "rationale": "A destination copied into a reference appears nowhere in the span that reference covers, so the tree stated a resolution result as though it were source. It also made every definition edit reparse every unit mentioning the label, through reference_payloads_equal and collect_dependents. Stating the destination once, at the definition, is what the incremental contract's source-faithful clause requires and what makes its cost bound reachable.",
            "handling": "The comparison applies upstream's model to this side: definitions are dropped and references are resolved against them. Which destination a reference resolves to is still compared, so resolving to the wrong one, or to none, is still a failure.",
            "evidence": "scripts/lib/upstream-cmark.mjs, applyUpstreamReferenceModel; docs/migration/2026-08-01-incremental-canonical-ast-plan.md, M6"
        },
        {
            "id": "footnote-call-label-bytes",
            "kind": "deliberate-difference",
            "summary": "Upstream builds a footnote call's label by slicing the FOLLOWING node's literal to a length computed in columns, and the two coordinate spaces are unrelated: a label crossing a line ending comes out empty and its bytes reach no node, and an entity-spelled caret indexes past the end of an owned decode buffer. Markdown Core slices the source between the two brackets, so the bytes an unresolved call degrades back to are the bytes the author wrote.",
            "rationale": "No source byte may reach no node. Upstream loses `a`, the newline and `b` from `x[^a\\nb] tail`, and loses the WHOLE paragraph from `x [&Hat;abcdefghij] y` \u2014 it emits an empty text node. It is also a use-after-free: the loop that frees the label's neighbouring nodes runs while the label still points into one of them, and every footnote lookup then reads it.",
            "handling": "Keyed to three inputs rather than projected: the slice is one expression in one function, not a model this side could apply to a tree, and a normalizer that rewrote labels would blind the gate to every real label difference.",
            "evidence": "packages/markdown-core/core/inlines.c, handle_close_bracket's noMatch footnote branch. ASan on the entity input, default allocator, before the fix: heap-use-after-free, READ of size 1 in markdown_core_map_lookup.",
            "history": "Registered at 0a.2, which fixed the MEMORY: the label became a slice of the source instead of a slice of a freed node sized in columns. Its spelling was still wrong then -- `[\\^abc] x` came back as `[^^abc] x` and `x [&Hat;...] y` as `x [^Hat;...] y` -- because the branch still opened a call on a caret the source does not spell literally. 0a.9 (D14) made the branch test the raw source byte, and neither opens a call now: both degrade to their own decoded text. Upstream still differs on all three inputs, so the entries stayed registered across that change, which is what their `pending` note said would happen."
        },
        {
            "id": "footnote-duplicate-definition",
            "kind": "deliberate-difference",
            "summary": "When one label is defined twice, upstream resolves to whichever definition CLOSED first and destroys the other, taking everything written inside it. A definition nested inside another closes first, so the outer one and its content vanish. Markdown Core resolves to the definition that OPENED first, and the one that does not win stays in the tree where it was written.",
            "rationale": "Two independent claims. Resolution order: document order is the order a reader resolves in, and it is already what two definitions at the same level do \u2014 close order only differs when one is nested, which is precisely where upstream's answer surprises. Retention: the loser is text the author wrote, and destroying it deletes content that has nothing to do with the duplicate label \u2014 `OUTER opens first` reached no node at all.",
            "handling": "Keyed to one input rather than projected: dropping the loser in the normalizer would blind the gate to whether the loser survived, which is half of what this entry claims.",
            "evidence": "packages/markdown-core/core/blocks.c, process_footnotes: registration on ENTER rather than EXIT, and the sweep that hands a still-parented definition back to the tree before the map frees what it names. cmark-gfm 0.29.0.gfm.13 reproduces both halves.",
            "scope": "Whether an UNREFERENCED definition is kept is a different question and is not answered here: it is still dropped, and the `footnote-resolution-model` entry above states the model that changes it."
        },
        {
            "id": "refdef-title-rewind",
            "kind": "deliberate-difference",
            "summary": "When a definition's title candidate is followed by non-whitespace (the title-rewind case), upstream keeps the scanned title in its reference map even though the title bytes stay paragraph text, so a reference to that label resolves with a title the definition does not have. Markdown Core drops the title with the rewind: scalar, map entry, and concrete spelling agree that the definition has none.",
            "example": "[foo]: /url + a following \"title\" ok line, then [foo]",
            "upstream": "Link title=\"title\"",
            "ours": "Link title=null",
            "rationale": "The CommonMark specification's own prose for this example says \"This is a link reference definition, but it has no title\", and the rewound bytes remain visible paragraph text \u2014 reading them a second time as a title states one set of bytes as two things. micromark resolves the reference without a title.",
            "handling": "Keyed to one input rather than projected: upstream's kept title is one assignment in its reference parser, not a model. Erasing resolved titles in the normalizer would blind the gate to every real title difference. REGISTERED at 0a.7, which is where the fix landed \u2014 the `pendingStep` below said Step 9, and \u00a74.2's defect schedule moved it forward.",
            "evidence": "packages/markdown-core/core/inlines.c, markdown_core_parse_reference_inline rewind branch; CommonMark spec.txt, the example above \"This is a link reference definition, but it has no title\".",
            "landed": "0a.7, together with D6 and D16. Landing them separately would have made this entry read `Link title=\"\"` for one commit and `Link title=null` for the next, because the reference path used to turn an absent title into an empty one on the way out of the map."
        },
        {
            "id": "tasklist-checked-marker",
            "kind": "deliberate-difference",
            "summary": "Upstream decides a task item's checked state with a substring search for `[x]` over the whole line, so any unchecked item whose text contains a literal `[x]` is reported checked. Markdown Core reads the marker the scanner actually matched.",
            "example": "- [ ] call me [x] later",
            "upstream": "ListItem checked=true",
            "ours": "ListItem checked=false",
            "handling": "Keyed to one input rather than projected: upstream's substring search is a bug in one function, not a model this side could apply to a tree. Erasing `checked` in the normalizer would blind the gate to every real checked-state difference. REGISTERED at 0a.11, which is where the fix landed. The `pendingStep` this entry used to carry said Step 4c, a step number that stopped existing when the plan was re-ordered; the defect schedule owns it as D24.",
            "evidence": "docs/reviews/2026-07-28-first-principles-review.md, finding I76, fixed in 7c5025d. Still present in upstream 0.29.0.gfm.13.",
            "landed": "0a.11, together with D15. Activating it needed a CORPUS ADDITION and not a JSON edit: check-upstream-parity.mjs keys expectedDivergences by input and fails any entry the corpus never reaches, and `- [ ] call me [x] later` was in no fixture at all. The example is now the last one in extensions.txt."
        },
        {
            "id": "empty-text-node",
            "kind": "deliberate-difference",
            "summary": "Upstream emits a `Text` node with no literal wherever a split leaves a fragment with nothing in it -- an autolink at column one, an autolink ending the run, and the spaces a hard break strips. Markdown Core emits no such node: a child with no literal and no source is not a node, and a parent that reports it as a child is lying about its own arity.",
            "example": "http://example.com  (an autolink at column one), and CommonMark example 564",
            "upstream": "Paragraph children=2, the first a Text literal=\"\"",
            "ours": "Paragraph children=1",
            "rationale": "Two independent claims. The node carries no bytes, so it has nothing to render and no source to point at -- the only position it can hold is borrowed or zeroed, and 36 of the 46 in this repository's own corpus held 0:0..0:0. And it makes a container's child count wrong, which is the one field a consumer cannot recover by looking harder. remark has never had the node; mdast is the second authority.",
            "handling": "PROJECTED, not keyed to inputs: it appears wherever the construct does, so a list of inputs would go stale the moment the corpus grew. `normalize` in scripts/lib/upstream-cmark.mjs drops an empty-literal `Text` from BOTH sides, after the adjacent-run join, so a merged run that came out empty goes with it.",
            "evidence": "packages/markdown-core/core/iterator.c, markdown_core_consolidate_text_nodes; packages/markdown-core/extensions/autolink.c, postprocess_text. Without the projection the gate reads 806/817 with eleven inputs diverging -- eight autolink, three hard-break and shortcut-reference.",
            "landed": "0a.14, and this entry IS Q38. The question was owed by that sub-step and the answer is the projection rather than eleven `expectedDivergences` rows. specs/mdast-parity/deltas.json carries the same id and its note is amended in the same commit, because the sentence that classified this as a shape delta rather than a defect became history here."
        }
    ],
    "expectedDivergences": [
        {
            "id": "footnote-call-label-bytes",
            "input": "x[^a\nb] tail\n",
            "note": "The label crosses a line ending. Upstream's column arithmetic underflows and it emits `x[^] tail`; the `a`, the newline and the `b` are in no node."
        },
        {
            "id": "footnote-call-label-bytes",
            "input": "x [&Hat;abcdefghij] y\n\n[^z]: note\n",
            "note": "The caret arrives as a character reference, so the node the label was sliced out of held an OWNED one-byte decode buffer. Upstream reads far past it and then frees it; the ASan witness for the use-after-free is this input, and upstream's own output for it is an empty text node \u2014 the whole paragraph is lost. This side opens no call at all as of 0a.9 and the entity decodes, giving `x [^abcdefghij] y`."
        },
        {
            "id": "footnote-call-label-bytes",
            "input": "[\\^abc] x\n",
            "note": "An escaped caret. Upstream manufactures a closing bracket it never read \u2014 `[^abc]] x`. This side opens no call at all as of 0a.9 and the escape simply decodes, giving `[^abc] x`."
        },
        {
            "id": "footnote-duplicate-definition",
            "input": "Ref [^dup].\n\n[^dup]: OUTER opens first\n\n    [^dup]: INNER closes first\n",
            "note": "The nested duplicate. Upstream keeps only `INNER closes first`; `OUTER opens first` is in no node."
        },
        {
            "id": "refdef-title-rewind",
            "input": "[foo]: /url\n\"title\" ok\n\n[foo]\n",
            "note": "Keyed to an input rather than projected: upstream's kept title is one assignment in its reference parser, not a model. Erasing resolved titles in the normalizer would blind the gate to every real title difference. ACTIVE as of 0a.7."
        },
        {
            "id": "footnote-call-label-bytes",
            "input": "a[^n] b\n\na[\\^n] b\n\na[&#94;n] b\n\na[&#x5E;n] b\n\na[&#x5e;n] b\n\na[&Hat;n] b\n\n[^n]: note\n",
            "note": "Six spellings of a caret against one label, with the definition present \u2014 the 432-case matrix reduced. Only the raw caret opens a call on this side, so five paragraphs decode to the identical text. Upstream recognizes all six, and the result is worse than a spelling difference: it emits `a[^n]] b` for the escaped one and then a single EMPTY text node for the rest, losing four paragraphs outright. Registered at 0a.9, which is the commit that made this side stop recognizing five of the six."
        },
        {
            "id": "tasklist-checked-marker",
            "input": "- [ ] call me [x] later\n",
            "note": "Keyed to an input rather than projected: upstream's substring search is a bug in one function, not a model this side could apply to a tree. Erasing `checked` in the normalizer would blind the gate to every real checked-state difference. ACTIVE as of 0a.11."
        }
    ],
    "notDeltas": [],
    "corpus": [
        "packages/markdown-core/tests/fixtures/spec.txt",
        "packages/markdown-core/tests/fixtures/regression.txt",
        "packages/markdown-core/tests/fixtures/extensions.txt",
        "packages/markdown-core/tests/fixtures/extensions-directive.txt",
        "packages/markdown-core/tests/fixtures/extensions-directive-option-gates.txt",
        "packages/markdown-core/tests/fixtures/extensions-conflicts.txt",
        "packages/markdown-core/tests/fixtures/extensions-formula-github.txt",
        "packages/markdown-core/tests/fixtures/extensions-formula-latex.txt",
        "packages/markdown-core/tests/fixtures/extensions-formula-conflicts.txt",
        "packages/markdown-core/tests/fixtures/extensions-formula-option-gates.txt",
        {
            "file": "packages/markdown-core/tests/fixtures/smart_punct.txt",
            "profile": "gfm-smart",
            "upstreamFlags": [
                "--smart"
            ],
            "note": "Both parsers default smart punctuation off, so this file is the one corpus entry that needs a profile: `--smart` upstream, `--profile gfm-smart` here."
        }
    ],
    "corpusNote": "Every golden fixture cmark-gfm can judge. The spec and regression files are the base language. The extension files are here for the negative property: this repository's own syntax is off under --profile gfm, so those inputs must parse exactly as upstream parses them \u2014 which is what proves the extensions are additive rather than a change to the shared language. A golden that quietly encoded a divergence would be caught here; a golden no oracle ever reads is only a record that the output has not changed.\n\nRe-pinned against the untouched 1.0 baseline (580d10c) on 2026-08-20. Two cross-link/embed fixtures left the corpus with the feature. Three registered divergences moved to `pendingDeltas`: each describes a fix the reconstruction has not made yet, so at the baseline this engine AGREES with upstream where the entry expects it to differ. Do not restore them by hand \u2014 each returns as part of the step named in its `pendingStep`.",
    "pendingDeltas": [
        {
            "id": "table-split-lead-spelling",
            "kind": "deliberate-difference",
            "summary": "When a table's header row is recovered from a multi-line paragraph, upstream runs the split-off lead through the cell pipe-unescape before inline parsing, so the lead's escapes are processed twice: a `\\\\|` renders as `|`. Markdown Core keeps the lead's authored spelling and lets the inline pass read it as any other paragraph \u2014 `\\\\|` is an escaped backslash and a literal pipe.",
            "example": "pre \\\\| lead, above a two-column table",
            "upstream": "Text literal=\"pre | lead\"",
            "ours": "Text literal=\"pre \\\\| lead\"",
            "rationale": "The lead is not table content; a single `\\|` converges either way, and the double-processed `\\\\|` diverges from what the same bytes mean in every plain paragraph. micromark parses the lead as a plain paragraph and agrees \u2014 the twin agreement inputs are in specs/mdast-parity/corpus.md.",
            "handling": "NOT YET REGISTERED. This divergence exists because a fix that has not been re-applied to the 1.0 baseline. The baseline agrees with upstream here, which is the defect. It returns to `deltas` in the step named by `pendingStep`, and that step is not done until this entry is back and reproducing.",
            "evidence": "packages/markdown-core/extensions/table.c, try_inserting_table_header_paragraph; upstream extensions/table.c:283-301 (unescape_pipes on the lead).",
            "pendingStep": "Step 10 \u2014 the split-off table lead"
        }
    ],
    "pendingExpectedDivergences": [
        {
            "id": "table-split-lead-spelling",
            "input": "pre \\\\| lead\n| a | b |\n| - | - |\n",
            "note": "Keyed to an input rather than projected: upstream's double unescape is one call in its table extension, not a model. Only a `\\\\|` in a split-off lead diverges \u2014 a single `\\|` decodes to `|` on both sides.  NOT ACTIVE at the 1.0 baseline: the fix that creates this divergence has not been re-applied, so this engine currently AGREES with upstream here \u2014 which is the defect. See `pendingDeltas` for the step that restores it."
        }
    ]
}
