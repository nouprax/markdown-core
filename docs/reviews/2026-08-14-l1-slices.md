# L1, sliced: the living tree, prose-warm

Status: IN PROGRESS on branch `living-tree-l1`. Executes milestone L1 of
`2026-08-13-living-tree-plan.md` §6. Evidence: a nine-agent read of the
post-L0 engine (2026-08-14) — six censuses (append path, aliasing, warm
parser, identity, oracles, fallback) and three independent slice
decompositions under a risk-first, an oracle-first and a value-first lens.
Where the three disagreed the order below follows the risk-first argument,
for the reason recorded in §2.

## 1 · What the censuses settled

Four facts decide the shape of everything below, and each was verified
against the working tree rather than inherited from an earlier document.

**Nothing borrows the source bytes.** Every byte the tree keeps is copied
out of the fed buffer before the feed returns: the line into `curline`, the
remainder into the node's content strbuf, inline literals into a chunk that
borrows the *parent's content* rather than the input, marks and concrete
records as integer offsets. Today's code already proved it — the joined
buffer was freed the instant `document_build` returned. So a chain-owned
buffer may grow and move without invalidating any document already built.

**`refine_blocks` is terminal, and it is the only thing that is.** It stamps
the tree, then does `res = parser->root; parser->root = NULL`, and a parser
whose root is NULL ignores every further feed. Nothing else in the parse
path is one-shot. A warm tick therefore may not call it; the fallback still
can, because the fallback builds its own parser.

**`finalize` is the sole writer of end coordinates, and there is no
un-finalize.** It asserts the node is OPEN before clearing the flag, and
`finalize_blocks` closes the whole spine to the root and destroys the held
partial line. A living tree must be closed to be *published* and must be
reopened to keep *growing* — which is the one claim in this milestone whose
failure has no cheap answer.

**Inline literals borrow the block's content buffer.** A unit that has been
refined holds children pointing into `node->content.ptr`; `add_line` grows
that buffer. So a warm tick's first act on the open leaf must be to retire
its inline children (and free its `inline_concrete`, which is a once-only
assignment), and only then feed bytes. This is the one genuine
use-after-free the milestone creates, and it is not at the byte layer.

## 2 · The order, and why

The risk-first lens named the claim that, if false, forces a redesign back
toward the buried fork: *a tick can speculatively close the open spine to
publish a correct projection, and then exactly undo that close so the next
tick continues as if it never happened.* Everything else in L1 — ownership
moving to the chain, ids minted at creation, revisions by touch — is a
refactor whose failure mode is a bug. That claim is provable COLD: with no
living tree, no ownership change, and no edit to `markdown_core_document_append`.
So it goes early, and the enabling refactors follow it rather than precede
it.

| # | Slice | Lands | Gate |
| --- | --- | --- | --- |
| 1 | **Chain-owned bytes** — DONE (`7abbf4d`) | One growable buffer on the chain; a document is a length watermark; the per-tick join and the per-document copy are gone | Correctness ×4 configs; the allocation sweep now covers the largest append allocation, which used to be bare `malloc` and invisible to injection |
| 2 | **Capture-then-compare** — DONE (`9cf1395`) | The append oracle records the head before the mutation and never reads it after | Killed by a mutant (revision never bumped): 14/86 fail on the reshaped clause |
| 3a | **The fingerprint** — DONE | `markdown_core_parser_warm_fingerprint`: everything a projection may read but must not change, over an explicitly enumerated field list — the decidable form of "the parser is exactly where it was" | Its own case: twins agree, the cut point does not matter, one more byte moves it, a held line differs from a processed one, a feed of no bytes does not move it, and reading it does not change it |
| 3b | **Close and undo, proven cold** — DONE | The claim itself, with ZERO production code: the undo lives in the runner, because what was in question is whether the state is restorable at all. every prose cut position closes, comes back bit-identical, and then continues to a warm state equal to an uninterrupted parse | Its own case, at every cut of six texts (one of them multi-byte, so cuts land inside characters): fingerprint before the close equals the one after the undo, and the continued parser's fingerprint equals a twin that was never interrupted. Mutant-checked twice (drop the line counter restore, drop the content truncate) |
| 4 | **Refine without ending the parse** — DONE | `markdown_core_parser_warm_refine`: the two passes refine_blocks runs, without the stamp and without taking the tree away, so a projection can be published mid-stream and the parser fed afterwards. Its inverse lives in the runner with the rest of the undo | The proof's chain grew a link: at every cut the closed tree is refined and its dump must equal a one-shot parse of the same prefix, before the refine is retired and the close undone. Mutant-checked by parsing the reference one byte short. The PER-UNIT form (refine each unit once as it settles, `b0e4662`'s `warm_refine_settled`) is deliberately deferred to slice 7, where re-refining the whole tree per tick would be the term that matters |
| 4.5 | **The ledger, and the gate that inverts** — DONE | The chain counts its ticks and the BYTES they rebuilt, exercised at full corpus before the path it will measure exists; Kotlin's reuse gate reads the receiver's extent before the append rather than after | The ledger's own case: a fresh chain has served nothing, four appends leave four rebuilt ticks, zero warm ticks, and rebuilt bytes equal to the running sum of the document's own lengths — today's quadratic, written down where changing it cannot go unnoticed |
| 5 | **The chain owns the tree** — DONE | A BUILD produces a generation — a tree, its diagnostics, and the arena they came from — and publishing swaps it in whole and releases what it replaced. The chain holds the head's generation plus the options and the pooling; a handle is `{chain, revision, length}` and owns nothing but its own cell. E3 is what made this one generation instead of two | The whole suite, since a wrong owner is a leak or a use-after-free rather than a different answer: correctness ×4 configs, `leaks` at zero on the chain-heavy runner, and ASan forcing pooling off so the per-structure teardown is the path exercised |
| 5.5 | **The identity counter moves to the chain** — DONE | `next_id` leaves the document: one counter per chain, minted from where identity actually lives. Behaviour-identical under D6 — the per-document copy was chaining the same number forward — and the failed-build case now burns numbers instead of reclaiming them, which is the right direction for a counter that must never reuse | The replay ledger, unchanged: a mutant that resets the counter per tick fails 13 of 89 tests, because ids start repeating and a retired id comes back |
| 5.75 | **A superseded handle answers for no tree** — DONE | The read right resolves to the strict reading (erratum E3 of the living-tree plan): the tree goes the moment a handle is superseded, and free plus revision/series/length remain. Root answers NULL, dump and diagnostics answer as for no document | The api chain case, rewritten to capture the head's dump before the mutation and then to assert the narrowing itself. One test paid for it; the bindings needed nothing, because all three decode at build time and never call back in |
| 6 | **Ids for what a warm tick creates** — RESHAPED, folded into 7 | The engine has 19 node-creation sites, and threading a counter through all of them buys nothing while D6 rebuilds every tick: the diff would immediately overwrite the ids of every paired node, and burn the counter for the rest. What "identity is object identity" actually needs is narrower — a node gets an id once and keeps it — and a warm tick already knows exactly which nodes it created, because the undo has to know. So the mint moves into the warm tick rather than into the constructors | The replay ledger, unchanged: an unminted node carries id 0 and fails on the first tick |
| 6.5 | **The tick shape, composed** — DONE | The cold proof run as a STREAM: one parser fed in 1, 3 and 7-byte pieces, with the whole tick shape at every boundary — close, refine, publish, retire, undo — and the parse continuing through all of them | 185 ticks over two texts and three strides: every projection equals a one-shot parse of the bytes so far, every undo comes back bit-identical, and the state at the end equals a parser that was never interrupted |
| 6.6 | **What slice 7 turned out to require** — ANSWERED | Derived while planning it, not while doing it. A warm tick may NOT re-refine the whole tree: the proof's un-refine frees every unit's inline children, so re-refining mints new ids for every inline node on every tick, and identity would come out WORSE than today's diff, which preserves them. So the warm tick needs the per-settle refine (`b0e4662`'s `warm_refine_settled`: refine each unit once as it settles, and only the open unit again per tick) — and that buried implementation carries two rules of its own. It refines against the definition tables AS FED, which agrees with a fresh parse only until a definition lands after a unit that mentioned its label, so a feed that grows either table must fall back. And it REFUSES parsers with extensions attached, because their postprocess hooks can replace a unit out from under the settle list — and every real parser here has eight attached. That last one is now answered — see 6.7 | — |
| 6.7 | **Which hooks may replace a unit** — SETTLED, and it found a use-after-free in the prototype | Exactly one hook can put a different node object at a unit's position: formula's `postprocess_block`, arms 2 and 3 — a fenced code block whose info is exactly `formula`, and a paragraph whose child list after the inline parse is one standalone display formula. Both free what they replaced. Everything else never does: autolink's hook splices a unit's CHILD list and returns the unit; the three `materialize_inline` hooks touch payload only; the other six extensions register no block hook; `consolidate_texts` can only free a TEXT that is a TEXT's sibling, and a unit is never TEXT. The structural fact that makes a settle list safe: both replaceable shapes are block LEAVES, so a replacement can only invalidate the entry being refined — never a neighbour, never a later one. **So `b0e4662`'s refusal was over-broad, and reviving it as written would have shipped a use-after-free**: its `warm_refine_settled` stamps `unit` after the pipeline may have freed it. What it needed was a return value. Three corrections follow — `S_postprocess_unit` must RETURN the survivor (it is `void` and swallows it today), `owns_inlines` must be recomputed from the returned node (arm 3 turns an inline-owning paragraph into a FormulaBlock), and any bookkeeping that caches CHILD pointers must run AFTER the postprocess, because autolink splices them | Four independent censuses and two adversarial refutation passes, neither of which could refute; a wrong answer here is a silent use-after-free rather than a wrong tree. The one shape a caller could not see — splice, then answer NULL — is now an assert |
| 6.8 | **The definition-table rule discharges itself** — SETTLED | The buried prototype's second rule — a warm tick must fall back the moment a feed grows either definition table, because refinement uses the tables AS FED — needs no new machinery here. Both tables grow at exactly two sites: the footnote table when a footnote definition OPENS (`core/blocks.c:1949`) and the link table inside the paragraph-finalize harvest (`core/inlines.c:2259`). Both require a `[`, and prose eligibility already rejects `[` in the held line and in the open paragraph's content, so a prose-eligible tick cannot reach either | The predicate the cold proof already runs |
| 6.9 | **Per-settle refine, with the survivor handed back** — DONE | `markdown_core_parser_warm_refine_settled(parser, unit)`: one unit's inlines and its own block-local postprocess, RETURNING the node now at that position — the correction the verification demanded, since a postprocessor may replace a unit and free what it replaced. `S_postprocess_unit` stops being `void` to make that possible. The stream proof now runs the real semantics: a unit closed by the FEED is refined once and keeps its inline children — and therefore its ids — for the rest of the stream, while only the units the speculative close closed are refined tentatively and retired at the undo | 185 ticks again, now with settled units never re-refined; the end state equals a twin fed once and settle-refined once, which says the incremental order leaves exactly what refining at the end would. The rule the first failure taught: SETTLED MEANS CLOSED — refining an open unit hangs inline children on a buffer the next line appends to, which is the borrow the censuses warned about |
| 7 | **The first warm tick** | Prose shapes only (paragraph continuation, blank line, new paragraph) behind an eligibility predicate decided BEFORE the first write of the tick; revisions by touch; the touched spine restamped so a later fallback's pairing stays sound | A differential mode that reparses every tick and compares, run over the equivalence and fuzz corpora, with fault injection proving the differential can fail |
| 8 | **The bound, gated** | The cost gate counts reparsed BYTES against appended bytes, not ticks — 5% of ticks each reparsing the document is not a 5% problem | Σ tick_cost / one_shot(N) flat across doublings on the prose shapes; the honest ladder's shapes record their documented degradation |

## 3 · Hazards the slices must answer

Each was raised by a census with file:line evidence and is not yet closed;
the slice that owns it is named.

- **The tree dies with the receiver** (slice 5). The tree is allocated from a
  per-document arena and the contract lets a caller free a superseded handle
  at any moment — including the ES and Kotlin reclaim paths, which do
  exactly that. The arena moves to the chain in the same motion as the tree.
- **Fallback after mutation** (slice 7). A line that begins as a paragraph
  continuation can turn out to be a setext underline, a table delimiter row
  or a fence close, by which point `add_line` has already written. The
  eligibility decision must be a pure probe over the arriving bytes and the
  open-spine shape, taken before any write.
- **Stale hashes corrupt the next fallback** (slice 7). A warm tick that
  mutates nodes without restamping the touched spine leaves `subtree_hash`
  lying, and the next fallback's pairing sweeps believe it.
- **The half line is not always truncatable** (slice 3/6). A held partial
  line that is a reference definition or a fence info line runs harvests and
  byte drops that a truncate cannot undo. L1 gates the warm path on the
  shape of the HELD line, not only on the containing block.
- ~~**Kotlin's reuse gate inverts**~~ — CLOSED in slice 4.5: the receiver's
  root extent is now read before the append, so the bound describes the
  receiver even once a successor can share its predecessor's root.
- **The local UBSan is blind to the whole zero-length-on-null class.**
  Apple's sanitizer reports neither a zero offset applied to a null pointer
  (slice 1's `buffer + len`) nor a null passed where a library declares its
  parameter non-null (slice 3b's `memcpy(dst, NULL, 0)`); the Linux
  toolchains CI runs report both, and both were caught there after a green
  local four-config run. Naming `pointer-overflow` explicitly does not help
  — measured: Apple clang accepts the flag and still says nothing. Empty
  buffers, empty chunks and empty counts must be reasoned about, not merely
  run: guard the length before the call, once, where the call lives.
- ~~**A hook nothing invokes**~~ — found by the 6.7 census and DELETED in
  the same breath. `markdown_core_prepare_inline_domain_func` had a typedef,
  a field in the extension descriptor, two implementations and two
  registrations, and no caller anywhere; its signature took a
  `committed_owner`, and the commit that last touched it is the one that
  deleted the incremental reuse machinery (#98). It was that machinery's
  last stranded reader.
- **The 39-entry inventory's coordinates have drifted.** L0 shifted
  `core/blocks.c`; the measured deltas are −8 for the line-processing sites,
  −6 for `finalize_blocks`, +1 for `finalize`/`add_child`. Verify every
  coordinate against the working tree before using it as a journal spec.

## 4 · What L1 does not do

No journal totality (that is L2): every shape outside the prose set falls
back per tick, and the fallback stays exactly as correct as today's append.
No deletion of `diff.c`, the hash apparatus or the stamp (that is L3, and
only once the fallback rate is zero). No intra-unit incremental inline
parsing, ever — the honest ladder keeps its prose-wall entry.
