{
    "name": "Markdown Core",
    "user": "ubuntu",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "install": "scripts/tooling/setup-environment.sh --install emscripten dependencies tools && scripts/tooling/setup-environment.sh --check core node java wrappers android emscripten dependencies tools"
}
