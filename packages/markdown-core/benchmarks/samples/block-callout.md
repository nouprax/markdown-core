> [!NOTE] Measured on one toolchain
> The lane pins the compiler and the C library, so a change in either
> moves the numbers without any parser change.

> [!WARNING]- Collapsed by default
> A corpus that moved cannot be compared against an earlier report.

> [!TIP]+ Expanded by default
> Narrow a moved corpus to the cases that changed with `--case`.
>
> > [!NOTE] Nested
> > A callout inside a callout keeps its own title.

> [!IMPORTANT]
> A callout with no title carries only its body.
