The lane is pinned <!-- to one toolchain --> so the numbers compare, and
%%the wall-clock lane%% was replaced by the staged one.

%%
A fenced comment block explaining why the corpus is repeated to a target
size rather than measured once at its authored length.
%%

<!-- An HTML comment block covering the same ground, so both block
comment spellings appear in the corpus. -->

Inline %%hidden%% notes and <!-- html comments --> appear in the same
paragraph as ordinary *emphasis*, so both comment scanners run on the
same line.

%%A longer hidden run that spans a clause and closes at the end.%%
