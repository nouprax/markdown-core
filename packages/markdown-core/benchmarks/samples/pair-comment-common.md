The lane is pinned ``to one toolchain`` so the numbers compare, and
``the wall-clock lane`` was replaced by the staged one.

Inline ``hidden`` notes sit beside ordinary *emphasis* and **strong**
runs, so the comment scanner shares each line with the delimiter stack
rather than running on a line of its own.

``A longer hidden run that spans a whole clause and closes at the end``
of the paragraph, and ``a`` very short one closes immediately.

Several runs ``one`` on ``two`` a ``three`` single ``four`` line keep
the scanner opening and closing without any prose between them.
