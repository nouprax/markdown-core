The [staged lane]{#lane .stage k="callgrind"} reports each phase, and the
[source stage]{.stage} and [AST stage]{.stage} are named separately.

Spans carry [nested *emphasis* and `code`]{#nested .one .two} as content,
while []{} and [plain]{} stay cheap, and [see [detail](/detail)]{.links}
holds a link inside the span body.

A [span]{#a .b k="v" data-kind="note"} with four attributes exercises the
attribute grammar past the single-class fast path, and repeated
[runs]{.r} [of]{.r} [short]{.r} [spans]{.r} keep the opener stack shallow.
